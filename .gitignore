# Сырые дампы словарей (несколько ГБ) — качаются скриптом, в репозитории не нужны.
# Именно каталог целиком: раскладка внутри по языкам, шаблон data/*.jsonl её не ловит.
data/

# Собранные словари, наоборот, хранятся — на VPS ничего парсить не надо:
!app/db/

.env
node_modules/
.DS_Store
*.part
